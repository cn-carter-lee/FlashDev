@{
    ViewBag.Title = "";
}
@section head {
    <script src="/Scripts/jquery-1.10.2.js"></script>
    <script src="/Scripts/jquery-ui-1.10.4.custom.js"></script>
    <script src="/Scripts/swfobject.js"></script>
    <script src="/Scripts/student.js"></script>
    <style type="text/css">
        .clear:after { content: '.'; clear: both; display: block; height: 0; visibility: hidden; font-size: 0; line-height: 0; }
        .tag_list { margin: 0; padding: 0; list-style-type: none; }
        .tag_list li { float: left; margin: 2px 3px 2px 0; position: relative; font-size: 11px; line-height: 13px; position: relative; }
        .tag_list li .name { background-color: #7148b2; border-radius: 11px 0 0 11px; float: left; margin-right: 16px; color: #fff; padding: 2px 4px 2px 6px; text-decoration: none; font-size: 11px; line-height: 13px; }
        .tag_list li .remove { position: absolute; top: 0; bottom: 0; right: 0; border-radius: 0 11px 11px 0; padding: 2px 4px 2px 6px; color: #e0e0e0; padding: 0; width: 16px; background-color: #588a00; border-left: 1px solid #b2d165; }
        .tag_list li .remove:after { float: left; font-size: 11px; line-height: 13px; content: "\00D7"; margin: 1px 0 0 3px; }
        
        
        .wrapper { position: relative; z-index: 3; }
        .wrapper .bubble { width: 310px; height: 0px; clear: both; position: absolute; overflow: hidden; }
        .wrapper.active .bubble { position: absolute; width: 300px; margin-top: 4px; margin-bottom: 20px; top: 25; height: 70px; font-size: 12px; color: #666; background-color: #fff; background-clip: padding-box; border: 1px solid #ccc; border: 1px solid rgba(0,0,0,0.15); border-radius: 5px; box-shadow: 0 3px 12px rgba(0,0,0,0.25); }
        
        .wrapper.active .title { padding: 2px 10px; background-color: #eeeeee; background-repeat: repeat-x; border-bottom: 1px solid #ddd; font-weight: bold; }
        .wrapper.active ul { list-style: none; margin: 0; padding: 0; }
        .wrapper.active li { float: left; color: #555; margin-left: 20px; margin-top: 0px; text-decoration: underline; }
        .wrapper.active li.selected { font-weight: bold; text-decoration: none; }
        
        .wrapper span { padding-left: 10px; }
        .wrapper span:before { position: absolute; top: 10px; right: 10px; display: block; width: 0; height: 0; content: ""; border: 4px solid; border-right-color: transparent; border-left-color: transparent; border-bottom-color: transparent; }
        .select-menu { width: 120px; height: 26px; line-height: 26px; cursor: pointer; border-radius: 3px; border: 1px solid #d5d5d5; text-shadow: 0 1px 0 rgba(255,255,255,0.9); background-image: linear-gradient(#fcfcfc, #eee); background-repeat: repeat-x; }
        
        .select-menu-over { width: 120px; height: 26px; line-height: 26px; cursor: pointer; border-radius: 3px; border: 1px solid #d5d5d5; text-shadow: 0 1px 0 rgba(255,255,255,0.9); background-image: linear-gradient(#fcfcfc, #ddd); background-repeat: repeat-x; }
    </style>
}
<div class="middle-container">
    <ul class="clear tag_list clear">
        <li><a href="#" class="name">*****</a> <a href="#" class="remove" title="删除"></a>
        </li>
        <li></li>
    </ul>
    <input type="button" id="btnAddTag" value="添加" />
    <div id="dd" class="wrapper clear" style="float: left;">
        <div class="select-menu">
            <span>
                <input type="text" id="txtAddTag" style="width: 50px; border: 1px; margin: 0; padding: 0;"
                    value="输入..." /></span>
            <div class="bubble">
                <div class="title">
                    请选择:</div>
                <ul>
                    <li>***1</li><li>**2</li><li>**3</li><li>**4</li><li>**5</li>
                </ul>
            </div>
        </div>
    </div>
    <div>
    </div>
    <div id="altContent">
        <h1>
            Chart</h1>
        <p>
            <a href="http://www.adobe.com/go/getflashplayer">Get Adobe Flash player</a></p>
    </div>
    <ul id="products">
    </ul>
    <input type="text" style="width: 200px; margin: 0; padding: 0; height: 20px; line-height: 20px;
        border-radius: 3px;" />
</div>
<script type="text/javascript">
    var uri = '/api/StudentApi/GetTags';
    function formatItem(item) {
        return item.TagName;
    }
    $(document).ready(function () {
        $("#btnAddTag").click(function () {
            var name = $("#txtAddTag").val();
            $('<li>', { html: '<a href="#" class="name">' + name + '</a> <a href="#" class="remove" title="删除"></a>' }).appendTo($('.tag_list'));
        });
    });
    /*
    $.getJSON(uri)
    .done(function (data) {
    $.each(data, function (key, item) {
    $('<li>', { text: formatItem(item) }).appendTo($('#products'));
    });
    });
    function formatItem(item) {
    return item.TagName;
    }

    function find() {
    var id = $('#prodId').val();
    $.getJSON(uri + '/' + id)
    .done(function (data) {
    $('#product').text(formatItem(data));
    })
    .fail(function (jqXHR, textStatus, err) {
    $('#product').text('Error: ' + err);
    });
    }
    */
</script>
