package
{
/*
   /* // Show errorr message
   var m:ErrorMsg = new ErrorMsg(msg)
   this.addChild(m);
   this.removeChildAt(0);
 */

/* // Add the 'loading data...' msg:
   var l:Loading = new Loading("Loading data...");
   this.addChild(l);
 */

/*
   var spin:Sprite = new Sprite();
   var radius:Number = 15;
   for (var i:Number = 0; i < 20; i++)
   {
   // var deg:Number = (360 / dots) * i;
   // var radians:Number = deg * (Math.PI / 180);
   // var x:Number = radius * Math.cos(radians);
   // var y:Number = radius * Math.sin(radians);

   spin.graphics.lineStyle(0, 0, 0);
   spin.graphics.beginFill(0xFF0000, 1);
   spin.graphics.drawCircle(20 * i, 400 - 20 * i, 2);
   }
   this.addChild(spin);
 */

	//throw new Error('test');

//			show_error("this is a wonderful world!!!!!");

/*
   // Test,draw an circle
   private function drawCircle()
   {
   var tf:TextField = new TextField();
   tf.text = "Hello World!";
   this.addChild(tf);
   this.graphics.lineStyle(2, 0xFF0000, 1);
   this.graphics.beginFill(0x00FF00, 1);
   this.graphics.drawRoundRect(0, 0, this.parent.stage.width, this.stage.height, 1000, 1000);
   Tweener.addTween(this, {x: 50, time: 0.3, transition: Equations.easeOutExpo});
   Tweener.addTween(this, {y: 50, time: 0.3, transition: Equations.easeOutExpo});
   }
 */
   
 
 
}